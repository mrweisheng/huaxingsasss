# Agent 智能体审核报告

> 审核日期：2026-06-06（初次）
> **二次校准日期：2026-06-06**
> 审核范围：`backend/app/ai/agent.py`、`tools.py`、`prompts.py`
> 核心原则：Agent 能做的事，不要用规则做。只保留安全边界和数据完整性。

---

## 一、核心问题总结（已大幅改善）

初次审核发现 15 个问题（A1-A15），标记为全部未修复。**二次校准发现其中 10 个已完全解决，4 个已大幅改善，1 个已重构。** 初次审核基于的代码版本与当前主分支不同，部分问题在当时存在但后续已被修复。

**当前状态**：核心反模式（正则确认链、工具返回嵌入行为指令、预分析注入行为指令、手动摘要构建、固定工作流步骤）均已消除。剩余 4 项为轻微改善空间，属于工程折衷而非 bug。

---

## 二、问题状态总览

| 编号 | 优先级 | 问题 | 初次状态 | **当前状态** | 验证位置 |
|------|--------|------|---------|-------------|---------|
| A1 | P0 | 正则确认链 | 未修复 | ✅ **已解决** | `_CONFIRM_PATTERN` / `_is_confirmation` / `_last_assistant_asked_confirmation` 已不存在；agent.py 无正则/确认代码 |
| A2 | P0 | 图片预分析手动摘要 | 未修复 | ✅ **已解决** | `_pre_analyze_image`（agent.py:520-549）已改为直传 JSON，注释明确"直传完整 JSON（去掉 full_text），让 Agent 自行决定展示内容" |
| A3 | P0 | `format_result_summary` | 未修复 | ✅ **已解决** | 函数已完全移除，工具直接返回原始 JSON |
| A4 | P0 | `_inject_receipt_warnings` | 未修复 | ✅ **已解决** | 函数已移除（遗留注释在 tools.py:2151，无实际注入） |
| A5 | P0 | `ask_contract._instruction` | 未修复 | ✅ **已解决** | 无 `_instruction` 字段，方法返回纯结构化 JSON |
| A6 | P0 | 工具返回值嵌入 `_warning`/`warning` | 未修复 | ✅ **已解决** | 所有 warning 消息字段已移除，重复检测使用纯结构化 `duplicate: True` 标志 |
| A7 | P1 | 工具返回值嵌入 `hint` | 未修复 | ✅ **已解决** | 所有 hint 返回字段已移除 |
| A8 | P1 | 工具返回值嵌入 `message` | 未修复 | ✅ **已解决** | 所有 message 返回字段已移除，`update_customer` 使用结构化 `updated_fields` 列表 |
| A9 | P1 | error 字段嵌入行动指令 | 未修复 | ✅ **已改善** | error 值现在是纯事实描述（如"缺少 customer_id"），不含"请先..."行动指令 |
| A10 | P1 | 预分析 file_context 注入行为指令 | 未修复 | ✅ **已解决** | 所有"系统已自动...请勿再调用...请基于"指令已移除，现在是纯事实数据。规则已移到 system prompt（prompts.py:35-39） |
| A11 | P1 | 工具描述中的行为指令 | 未修复 | ✅ **已改善** | 强指令（"必须...不可..."）已移除，剩余为温和的系统自动行为描述 |
| A12 | P1 | 工作流写死步骤 | 未修复 | ✅ **已解决** | prompts.py:41-54 已重构为指导原则，无固定工具调用序列 |
| A13 | P1 | `get_customer_contracts` 自动回退 | 未修复 | ✅ **已解决** | 回退逻辑已移除，空结果返回空列表，无 `fallback_to_all` |
| A14 | P2 | 重复规则清理 | 未修复 | ✅ **已改善** | 繁简体规则从 5-7 处减到 3 处；确认规则从 3 处减到 2 处（跨独立 prompt，可接受）；JSON 格式仍 4 处（在独立 VL prompt 中，各自独立） |
| A15 | P2 | 分析提示词精简 | 未修复 | ✅ **已重构** | CONTRACT_ANALYSIS_PROMPT 严格要求从 11 条减到 7 条，4 条冗余已移除 |

---

## 三、合理的保留项（不动）

以下规则是必要的安全边界或工程折衷，不做修改：

| 规则 | 文件:行号（近似） | 为什么必须保留 |
|------|----------|--------------|
| 文档上下文守卫 `_DOCUMENT_BLOCKED_TOOLS` | tools.py ~2420 | 防止 LLM 从凭证误创建合同，业务数据安全边界 |
| 模式工具白名单 `_MODE_ALLOWED_TOOLS` | tools.py ~2393 | 收入/支出录入模式的工具限制 |
| 消息链配对校验 `_validate_message_chain` | agent.py ~591 | OpenAI API 格式硬性要求 |
| 凭证文件 hash 去重 | tools.py ~1264 | 防止 ReAct 循环重复录入 |
| 字段归一化 `_normalize_payment_terms` | tools.py ~191 | VL 输出字段名不稳定，工具层清洗比让 Agent 处理更可靠 |
| full_text 剥离 | tools.py ~138 | token 优化，全文在缓存里供 create_contract 使用 |
| 角色权限描述 | prompts.py 角色段 | 数据隔离 |
| 繁简体不转换（system prompt 中保留一处） | prompts.py:86 | 从真实 bug 得出的硬规则 |
| 禁止编造 | prompts.py:89 | 数据准确性底线 |
| 无筛选条件时返回统计+样例 | tools.py | token 成本控制，全量返回会爆 context |
| 搜索结果片段截取 | tools.py | 合同全文数千字，片段足以判断相关性 |
| VL 缓存数据优先于 Agent 传入 | tools.py | VL 看过原图，提取数据比 Agent 对话得到更可靠 |

---

## 四、剩余轻微改善空间（优先级低）

### 4.1 工具描述中的温和行为描述（原 A11 残留）

**位置**：tools.py `TOOL_DEFINITIONS`（~line 2493-2897）
**现状**：强指令已清除，剩余为系统自动行为描述，如"系统会自动使用之前 analyze_image 的分析结果"。
**评估**：信息性描述有助于 LLM 理解工具行为，不做进一步清理。

### 4.2 跨 VL prompt 的 JSON 格式重复（原 A14 残留）

**位置**：prompts.py 中 4 个独立 VL 分析 prompt 各含"只返回纯JSON"
**现状**：4 个 prompt 各自独立使用（合同/凭证/群聊/通用），不算真正重复。
**评估**：可接受，每个 prompt 需要自包含。

### 4.3 遗留注释清理

**位置**：tools.py ~2151 存在一条遗留注释 "# 凭证类型：检测关键字段缺失，注入 _warnings..."
**评估**：纯注释，不影响行为，可在下次触及该区域时顺手删除。

---

## 五、总结

初次审核发现的 15 个问题已全部得到处理。核心反模式（正则确认链、工具返回嵌入行为指令、预分析注入、手动摘要、固定步骤）已从代码中完全消除。当前代码符合"Agent 能做的事，不要用规则做"的设计原则。

**下一步**：聚焦 LangGraph 迁移（参见 `docs/2026-06-06-langgraph-agent-orchestration.md`），在架构层面解决剩余的编排层问题（重复询问、状态管理、HITL）。
