# 2026-06-02 工作日志

## Bug Fix: Agent 路径 create_contract 丢失 signed_date，以及根治重构

**问题**：通过 AI Agent 对话创建合同时，签订日期（signed_date）偶发性丢失。买车合同 HT202606021145559EC1 的 signed_date 为空，但两地牌合同 HT20260602114813E545 有值。

**根因**：`backend/app/ai/tools.py` `create_contract` 方法中，VL→Agent→代码 的三层数据流中，各字段的回退策略不一致——有些从 merged（VL缓存）回退，有些只读 kwargs（Agent传参）。`signed_date`/`start_date`/`end_date` 属于后者，Agent 没传就丢了。

**修复 + 重构**（tools.py）：
1. 引入 `_resolve(kwargs_key, merged_path, parser, default)` 统一取值函数：Agent 显式传入优先，否则走 VL 缓存回退路径
2. 用声明式 `contract_fields` 字典替代零散的字段赋值，新增 VL 字段只需加一行
3. `start_date`/`end_date` 额外处理 `validity_period` 嵌套路径（VL 输出结构如此）
4. `_parse_date()` 兼容 date 对象、字符串、datetime 多种格式

**根治原则**：VL 已正确提取的数据，不应因 Agent 漏传参数而丢失。所有 VL→ContractField 映射都应有缓存回退。

**仍然存在的已知 Gap**：
- `confidence` 模型字段：VL 有提取、存入了 contract_data JSON，但从未写入 `contract.confidence` 列（两条路径都如此）
- `needs_review` 模型字段：同样未被设置

## 第二轮修复：confidence + needs_review 补齐

**涉及文件**（3 个）：
1. `backend/app/ai/tools.py` — Agent 路径 create_contract：从 merged 提取 confidence，设置 contract.confidence 和 contract.needs_review（<0.85），同时加入返回值
2. `backend/app/tasks/contract_tasks.py` — REST API 路径：设置 contract.confidence 和 contract.needs_review
3. `backend/app/services/contract_service.py` — update_contract_data 路径：同样补齐

**确认**：API 层 `contracts.py:175` 已在读取 `contract.confidence` 用于展示，修复有价值。

**审计结论**：VL 输出 20 个字段 → Contract 模型 15 个列，映射已全部闭环，无遗漏。

## 第四轮：逾期概念全量清除 + 收付管理页面改造

**决策**：逾期不是独立状态（仅计算），从系统源头彻底移除：
- `prompts.py`：移除所有逾期描述、get_overdue_payments 推荐、封面示例
- `tools.py`：移除 overdue_only 过滤、get_overdue_payments 整函数+工具定义、get_summary/get_overview 中逾期统计

**PaymentList 页面改造**：
1. KPI 区改为按币种+类型原币分组（不再混算 CNY 折算）
2. 新增币种标签列，金额列加宽
3. 移除逾期 KPI 卡片、逾期筛选选项、逾期行样式
4. grid 改为 auto-fill 动态列适配多种币种
5. 状态文案统一：待确认/已确认（不再叫待支付/已支付）

**AgentChat + ContractDetail + CSS 清理**：移除所有逾期 skill 映射、建议文案、状态映射、样式

**问题 3（设计缺陷）**：_resolve 返回 default 时不应用 parser。
→ **已修复**。default 也走 parser 保证类型一致。

---

## 第五轮：群聊截图关联合同业务类型过滤修复

**问题**：群聊截图识别为买车群后，`get_customer_contracts` 无差别返回全部合同（含不相关的两地牌合同），让用户选。

**原方案**（用户自行提交 c63b488）：
- `business_types.py` 单一事实来源 + legacy 映射
- `get_customer_contracts` 加 `business_type` 参数，零结果自动回退
- `GENERAL_ANALYSIS_PROMPT` 加 `business_type` 字段
- `GROUP_CHAT_ANALYSIS_PROMPT` 独立群聊 prompt
- System Prompt 加群聊流程规则
- `update_contract` 加审计来源

**审核发现的问题与修复**：
1. **P0** `_check_document_guard` 有 12 行不可达死代码（return 后残留）→ **已删除**
2. **P0** `GROUP_CHAT_ANALYSIS_PROMPT` 定义了但 `analyze_image` 从未引用 → **已接入**：import + prompt 选择器 + `_DOCUMENT_BLOCKED_TOOLS` + `_check_document_guard` label/hint 映射 + System Prompt 改为 `analysis_type="group_chat"`
3. **P1** `update_contract` 异常/提前返回时 `_document_context` 未清空 → **已修复**：3 个出口（no updates / service 返回 None / except）均加 `self._document_context = None`
4. 注释同步更新（审计来源注释、execute 方法注释）

---

## 第六轮：收付管理 KPI 区域重构

**问题**：`pl-kpi-section` 设计不合理 — (1) 主卡片+sub-row 重复展示同一币种数据，(2) 无视觉重心，(3) 收入/支出 UI 一模一样的卡片结构。

**重构方案**：
- **消除冗余**：删除 `pl-kpi-sub-row`/`pl-kpi-mini` 整行，币种数据只在面板内展示一次
- **建立视觉重心**：两个主面板（收入/支出）为主体，底部状态栏为辅助
- **收支差异化**：
  - 收入：顶部绿色边框、右上角圆形水印、向上箭头 SVG 图标、绿色渐变顶→底
  - 支出：顶部红色边框、**左下角**圆形水印（位置不同）、向下箭头 SVG 图标、红色渐变
  - 不只是颜色不同——装饰元素位置、箭头方向都不同
- **状态栏**：新设计 `pl-kpi-status-bar` + `pl-kpi-chip`，按状态分色圆点+标签+数量

**改动文件**：
- `PaymentList.tsx`：新增 `incomeGroups`/`expenseGroups`/`incomeCount`/`expenseCount`/`statusCounts` 的 useMemo，KPI JSX 完全重写
- `PaymentList.css`：旧 KPI 样式（~230行）完全替换为新设计，响应式同步更新
